apiVersion: v1
kind: Service
metadata:
  name: myproject-customer-service
spec:
  selector:
    app: myproject-customer-service
  type: LoadBalancer
  ports:
   -  protocol: TCP
      port: 5000
      targetPort: 5000